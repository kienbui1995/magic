# Root pytest config so `pytest connectors packs` works from the repo root
# (CI runs it that way). Each connector/pack also ships its own pytest.ini for
# running its suite standalone from inside its directory.
[pytest]
asyncio_mode = auto
